Создается файл includes.cache, там содержаться пути до необходимых файлов:

1. Заголовочный файл "Arduino.h"
     в нем содержаться описание функций, таких как:
	pinMode()
	ditalRead()
	digitalWrite()
	shiftOut()
	shiftIn()
      далее там идет подключение другой библиотеки "pins_arduino"
      кроме того, в ней есть "avr/io.h"
1.1. "avr/io.h"

2. Заголовочный файл "pins_arduino"
Тут содержатся 